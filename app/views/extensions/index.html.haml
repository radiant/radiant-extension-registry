- @title = "Radiant CMS Extensions"
- @h1 = (@controller.action_name == 'index') ? "Extensions" : "All Extensions"
- @selected = :extensions

- content_for :actions do
  = link_to( image_tag("/images/plus.png", :class=>"icon") +" Add Extension", new_extension_path, :class => "button")

.view_order
  View
  = view_orders(@order)
  
.records_count= record_count_for @extensions, Extension

= pagination_for @extensions

%table.index#extensions{:style => "clear: both"}
  %tbody
    = render :partial => "extension", :collection => @extensions

= pagination_for @extensions